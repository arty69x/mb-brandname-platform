import Image from 'next/image'; import { heroImages } from '@/lib/data/products'; export default function Hero(){return <section className='py-16'><div className='container mx-auto px-4'><div className='grid gap-6 lg:grid-cols-2'>{heroImages.slice(0,2).map((img,idx)=><div key={img} className='relative h-80 overflow-hidden rounded lg:h-96'><Image src={`${img}?w=2200`} alt={`Hero ${idx+1}`} fill className='object-cover'/></div>)}</div></div></section>; }
