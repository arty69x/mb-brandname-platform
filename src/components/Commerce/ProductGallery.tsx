import Image from 'next/image'; export default function ProductGallery({ images, title }: { images: string[]; title: string }) { if(!Array.isArray(images)||images.length===0){return <p>No images available.</p>} return <div className='grid gap-4'><div className='relative aspect-square overflow-hidden rounded border'><Image src={`${images[0]}?w=1000`} alt={title} fill className='object-cover'/></div><div className='grid grid-cols-3 gap-4'>{images.slice(0,3).map((img)=> <div key={img} className='relative aspect-square overflow-hidden rounded border'><Image src={`${img}?w=500`} alt={`${title} thumbnail`} fill className='object-cover'/></div>)}</div></div>; }
