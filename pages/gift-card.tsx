import { useState } from 'react'; import Layout from '@/components/layout/Layout'; import SEO from '@/components/layout/SEO'; import PageTitleBlock from '@/components/ui/PageTitleBlock'; import Input from '@/components/ui/Input'; import Textarea from '@/components/ui/Textarea'; import Button from '@/components/ui/Button'; import { addToCart } from '@/lib/cart'; import { canonical } from '@/lib/seo';
export default function GiftCard() {const [amount,setAmount]=useState(50);const [email,setEmail]=useState('');const [sender,setSender]=useState('');const [msg,setMsg]=useState('');
return <Layout><SEO title="Gift Card — MB BRANDNAME" description="Gift cards for MB BRANDNAME." canonical={canonical('/gift-card')} /><main><section><div className="container mx-auto px-4"><PageTitleBlock title="GIFT CARD" /><div className="max-w-xl border p-6 space-y-4"><div className="flex gap-2">{[50,100,200,500].map((v)=><button key={v} onClick={()=>setAmount(v)} className="rounded-full border px-4 py-2">{v}</button>)}</div><Input id="recipient" label="Recipient Email" value={email} onChange={setEmail} /><Input id="sender" label="Sender Name" value={sender} onChange={setSender} /><Textarea id="msg" label="Message" value={msg} onChange={setMsg} /><Button onClick={()=>addToCart(`giftcard_${amount}`,1)}>ADD GIFT CARD TO CART</Button></div></div></section></main></Layout>;}
