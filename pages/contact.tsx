import { useState } from 'react';
import Layout from '@/components/layout/Layout'; import SEO from '@/components/layout/SEO'; import PageTitleBlock from '@/components/ui/PageTitleBlock'; import Input from '@/components/ui/Input'; import Textarea from '@/components/ui/Textarea'; import Button from '@/components/ui/Button'; import { canonical } from '@/lib/seo';
export default function Contact() {const [status,setStatus]=useState<'idle'|'loading'|'success'|'error'>('idle');const [fullName,setFullName]=useState('');const [email,setEmail]=useState('');const [subject,setSubject]=useState('');const [message,setMessage]=useState('');
const submit=async():Promise<void>=>{try{setStatus('loading');await new Promise((r)=>setTimeout(r,200));setStatus('success');}catch{setStatus('error');}};
return <Layout><SEO title="Contact — MB BRANDNAME" description="Contact MB BRANDNAME." canonical={canonical('/contact')} /><main><section><div className="container mx-auto px-4"><PageTitleBlock title="CONTACT US" /><div className="grid grid-cols-1 gap-8 lg:grid-cols-2"><div className="space-y-4"><Input id="fullName" label="Full Name" value={fullName} onChange={setFullName} /><Input id="emailC" label="Email" value={email} onChange={setEmail} /><Input id="subject" label="Subject" value={subject} onChange={setSubject} /><Textarea id="message" label="Message" value={message} onChange={setMessage} /><Button onClick={()=>{void submit();}}>SEND</Button>{status==='loading'?<p>Sending...</p>:null}{status==='success'?<p>Sent successfully.</p>:null}{status==='error'?<p>Error. Retry.</p>:null}</div><div><p>1418 River Drive, Suite 35 Cottonhall, CA 9622, United States</p><p>sale@uomo.com</p><p>+1 246-345-0695</p></div></div></div></section></main></Layout>;}
