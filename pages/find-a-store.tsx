import { useState } from 'react'; import Layout from '@/components/layout/Layout'; import SEO from '@/components/layout/SEO'; import PageTitleBlock from '@/components/ui/PageTitleBlock'; import Input from '@/components/ui/Input'; import { stores } from '@/data/stores'; import { canonical } from '@/lib/seo';
export default function FindStore() {const [q,setQ]=useState(''); const filtered = stores.filter((s)=>`${s.name} ${s.city} ${s.country}`.toLowerCase().includes(q.toLowerCase()));
return <Layout><SEO title="Find a Store — MB BRANDNAME" description="Find MB BRANDNAME stores." canonical={canonical('/find-a-store')} /><main><section><div className="container mx-auto px-4"><PageTitleBlock title="FIND A STORE" /><div className="max-w-[720px]"><Input id="search-store" label="Search" value={q} onChange={setQ} /></div><div className="mt-8 grid grid-cols-1 gap-4">{filtered.map((s)=><div key={s.name} className="border p-4"><p>{s.name}</p><p>{s.city}, {s.country}</p><p>{s.address}</p></div>)}</div></div></section></main></Layout>;}
