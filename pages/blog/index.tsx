import Link from 'next/link'; import Layout from '@/components/layout/Layout'; import SEO from '@/components/layout/SEO'; import PageTitleBlock from '@/components/ui/PageTitleBlock'; import { blogPosts } from '@/data/blog'; import { canonical } from '@/lib/seo';
export default function BlogIndex() {return <Layout><SEO title="Blog — MB BRANDNAME" description="Editorial and styling guides from MB BRANDNAME." canonical={canonical('/blog')} /><main><section><div className="container mx-auto px-4"><PageTitleBlock title="BLOG" /><div className="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-3">{blogPosts.map((post)=><article key={post.slug}><img src={post.image} alt={post.title} className="aspect-square w-full object-cover" /><h2 className="mt-4"><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p className="text-[var(--muted)]">{post.excerpt}</p></article>)}</div></div></section></main></Layout>;}
